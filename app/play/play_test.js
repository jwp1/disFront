'use strict';

describe('brainstrom.play module', function() {

  beforeEach(module('brainstrom.play'));

  describe('play controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var playCtrl = $controller('PlayCtrl');
      expect(playCtrl).toBeDefined();
    }));

  });
});