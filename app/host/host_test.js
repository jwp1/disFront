'use strict';

describe('brainstrom.host module', function() {

  beforeEach(module('brainstrom.host'));

  describe('host controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var hostCtrl = $controller('View3Ctrl');
      expect(hostCtrl).toBeDefined();
    }));

  });
});