'use strict';

describe('brainstrom.menu module', function() {

  beforeEach(module('brainstrom.menu'));

  describe('menu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var menuCtrl = $controller('View2Ctrl');
      expect(menuCtrl).toBeDefined();
    }));

  });
});